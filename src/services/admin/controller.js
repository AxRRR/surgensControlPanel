

const authLogin = async(req, res) => {
    // const payload = await getClans();

    // return res.status(200).json({
    //     status: true,
    //     payload
    // })
}

module.exports = {
    authLogin
};